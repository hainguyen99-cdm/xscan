import { Model } from 'mongoose';
import { UserDocument } from '../users/schemas/user.schema';
import { TransactionDocument } from '../payments/schemas/transaction.schema';
import { DonationDocument } from '../donations/schemas/donation.schema';
import { OBSSettingsDocument } from '../obs-settings/obs-settings.schema';
import { ExportFormatDto } from './dto/admin.dto';
export declare class AdminReportingService {
    private userModel;
    private transactionModel;
    private donationModel;
    private obsSettingsModel;
    constructor(userModel: Model<UserDocument>, transactionModel: Model<TransactionDocument>, donationModel: Model<DonationDocument>, obsSettingsModel: Model<OBSSettingsDocument>);
    generateRevenueReport(period: string, adminId: string): Promise<any>;
    generateGrowthReport(period: string, adminId: string): Promise<any>;
    generateComprehensiveReport(period: string, adminId: string): Promise<any>;
    exportData(format: string, exportData: ExportFormatDto, adminId: string): Promise<Buffer>;
    private exportUsersData;
    private exportTransactionsData;
    private exportDonationsData;
    private exportReportsData;
    private getUserStatistics;
    private getTransactionStatistics;
    private getDonationStatistics;
    private getOBSStatistics;
    private getDateRange;
    private convertToCSV;
    private convertToPDF;
    private convertToExcel;
    private getNestedValue;
}
